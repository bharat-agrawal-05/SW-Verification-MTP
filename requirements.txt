# Core dependencies for Loop Invariant Generation & Fixing framework
z3-solver>=4.12.0
apted>=1.0.3
pyyaml>=6.0
requests>=2.31.0
tqdm>=4.66.0
pydantic>=2.0.0

# LLM & Similarity backends
ollama>=0.3.0
sentence-transformers>=2.2.0
transformers>=4.40.0
torch>=2.0.0
accelerate>=0.28.0

# Optional for GPU acceleration / quantization on NVIDIA A5000 (24GB)
# bitsandbytes>=0.43.0
# vllm>=0.4.0

# Testing & formatting
pytest>=8.0.0
tabulate>=0.9.0
